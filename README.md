# Streamlit-Autoform
A pure-Python Streamlit component that auto-generates beautiful, multi-column forms with inline validation directly from Pydantic v2 models. Stop writing repetitive UI boilerplate for text inputs, sliders, and checkboxes—define your data schema once and instantly capture clean, structured human input.
